# $ZDOT_USER/conf.d/zle
#
# The file containing the settings for the zsh line editor (ZLE)
#
# Do:
#       - configure the ZLE :)
#
# Do not:
#       - set anything else here
#

zmodload -i zsh/zle		# Zsh Line Editor, includes bindkey and vared
                      # builtins

# If VISUAL or EDITOR contain 'vi' then the shell assumes 'viins'
# bindings (bindkey -v), otherwise 'emacs' is the default
# (bindkey -e). Uncomment the below line to override:
#bindkey -v
